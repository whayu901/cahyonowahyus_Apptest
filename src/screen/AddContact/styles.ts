import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imageProfile: {
    width: 120,
    height: 150,
    aspectRatio: 1 * 1.4,
    borderRadius: 15,
    marginTop: 5,
  },
});

export default styles;
