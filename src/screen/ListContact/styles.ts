import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  imageProfile: {
    width: 120,
    height: 120,
    borderRadius: 10,
  },
});

export default styles;
